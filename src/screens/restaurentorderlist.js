import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Restaurentorderlist = () => {
  return (
    <View>
      <Text>From restaurent order</Text>
    </View>
  );
};

export default Restaurentorderlist;

const styles = StyleSheet.create({});
